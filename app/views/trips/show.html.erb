<p id="notice"><%= notice %></p>
<div class="jumbotron">
  <div class="container">
    <ul class="media-list">
      <li class="media">
        <h3><b><%= @trip.county + " - " + @trip.location %> </br><%= @trip.leaders %></b></h3>

         <a class="pull-left">
                        <img class="media-object" <%=  image_tag  @trip.picture_url  %> </img>
        </a>
    <div class="media-body">
      <h4 class="media-heading">
        <b>From <%= @trip.start_date %> To <%= @trip.end_date %> </br>
          Trip Difficulty:  <% num = @trip.boots %>
                 <%= until num == 0 do %>
                    <% num -=1 %>
                     <%=  image_tag("boot.jpg", :akt=>"boot")  %>
              <%end%></br>
          Cost = £<%= @trip.cost %></b></h3>
      <h4><%= @trip.description %></h4>
      
    </div>
  </li>
</ul>
<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>

